In order to not push changes to local config, run the following command:
```bash
git untrack local/*
```

To undo the above command, run:
```bash
git track local/*
```

See the git file in this repo for the full commands (the above are aliases).
